# 依存関係
/node_modules
# Lambda 関数の依存関係（CDK bundling が npm install を実行するため不要）
amplify/functions/**/node_modules/
amplify/functions/**/package-lock.json
/.pnp
.pnp.js
.yarn/install-state.gz

# テスト
/coverage

# Next.js
/.next/
/out/

# ビルド
/build

# その他
.DS_Store
*.pem

# デバッグ
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# ローカル環境変数
.env*.local

# TypeScript
*.tsbuildinfo
next-env.d.ts

# Amplify
.amplify
amplify_outputs*
amplifyconfiguration*

# Python / エージェント
agents/.venv/
agents/__pycache__/
agents/**/__pycache__/
agents/*.egg-info/
agents/.env
*.pyc

# AgentCore CLI
agents/agentcore/.env.local
agents/agentcore/deployed-state.json
agents/agentcore/cdk/cdk.out/
agents/agentcore/cdk/node_modules/

# Notes (private)
notes/

# Legacy (Starter Toolkit)
agents/.bedrock_agentcore.yaml
agents/.bedrock_agentcore/

# Vector Search 比較検証: Ground_Truth キャッシュ
# Vector_Table の全件スキャン（約 28,500 RRU）から再生成できるローカルキャッシュ。
# 1 ファイル約 104 MiB（合計約 208 MiB）あり、git 履歴に入れる価値がないため除外する。
# 再生成: npm run vector:recall -- --refresh-ground-truth（この行を削除しないこと）
docs/measurements/ground-truth/

# Load Generator CDK
load-generator/cdk/cdk.out/
load-generator/cdk/cdk.context.json
load-generator/cdk/.venv/
