/**
 * amplify_outputs.json の型宣言
 *
 * このファイルは sandbox / デプロイ時に自動生成されるため、
 * 型チェックでエラーにならないよう宣言しておく。
 */
declare module "@/amplify_outputs.json" {
  const outputs: Record<string, unknown>;
  export default outputs;
}
